{
  "name": "push-manager",
  "private": true,
  "workspaces": [
    "src/web",
    "src/server",
    "src/shared"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "dev:web": "dotenv -e src/shared/.env yarn workspace @push-manager/web dev",
    "dev:server": "dotenv -e src/shared/.env yarn workspace @push-manager/server dev",
    "prod:web": "dotenv -e src/shared/.env yarn workspace @push-manager/web start",
    "prod:server": "dotenv -e src/shared/.env yarn workspace @push-manager/server start",
    "lint": "yarn workspaces run lint"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "cross-env": "^7.0.3",
    "dotenv-cli": "^7.4.1",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
